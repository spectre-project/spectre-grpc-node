{
  "name": "@spectre/grpc-node",
  "version": "1.0.9",
  "description": "Node gRPC for spectre-wallet",
  "main": "dist/index.js",
  "authors": [
    {
      "name": "ASPECTRON Inc."
    },
    {
      "name": "Spectre"
    }
  ],
  "license": "ISC",
  "scripts": {
    "watch": "tsc --watch",
    "prepublishOnly": "rm -rf ./dist && rm -f package-lock.json && npm install --also=dev && tsc",
    "prepare": "tsc"
  },
  "dependencies": {
    "@spectre/grpc": "github:spectre-project/spectre-grpc"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "typescript": "^4.0.5"
  },
  "files": [
    "dist",
    "lib",
    "types",
    "index.ts",
    "tsconfig.json"
  ]
}
